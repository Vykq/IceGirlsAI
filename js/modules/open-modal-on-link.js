const openModalOnLink = () => {

    const url = window.location.search;
    const modals = document.querySelectorAll('.modals');

    if(url.startsWith('?logout')){

    }
}

export default openModalOnLink;